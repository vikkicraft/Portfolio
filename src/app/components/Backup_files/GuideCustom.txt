
<custom corsor enable disable>
Disable CustomCursor (simplest)
In src/app/components/Layout.tsx, remove or comment out the <CustomCursor />
<custom corsor enable disable/>

<Theme change>
To change the default theme manually, edit line 21 in /src/app/components/Navbar.tsx
Change "dark" to "light" or vice-versa
example
Dark: const DEFAULT_THEME: "dark" | "light" = "dark";
Light:const DEFAULT_THEME: "dark" | "light" = "light";
<Theme change/>

<Thanos snap enable>
Here's what you need to do:

Step 1 — Create ParticleCanvas.tsx
Create a new file at src/app/components/ParticleCanvas.tsx and
paste the full contents from src/app/components/Backup_files/ParticleCanvas.tsx
into it (copy it exactly as-is).

Step 2 — Replace Skills.tsx
Replace the entire contents of src/app/components/Skills.tsx
with the full contents from src/app/components/Backup_files/Skills.tsx.

The only change to make after pasting: on line 147, change:

h3 className="font-sans text-xl sm:text-2xl ...
to:
h3 className="font-sans font-medium text-xl sm:text-2xl ...
  
(add font-medium to match the rest of the site's heading style)

Step 3 — Install the dependency
Run this in your terminal:

pnpm add simplex-noise
"simplex-noise": "^4.0.3",

That's it. The snap icon will appear next to "Skills and Expertise" —
click it once to disintegrate cards 2, 5, and 6, and click again to restore them.

<Thanos snap enable/>